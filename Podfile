# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ID3D iOS mid-level SwiftUI Test 03' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ID3D iOS mid-level SwiftUI Test 03

  # pod for zipping with advanced functionality to append files to an archive https://github.com/weichsel/ZIPFoundation
  pod 'ZIPFoundation', '~> 0.9'

  target 'ID3D iOS mid-level SwiftUI Test 03Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ID3D iOS mid-level SwiftUI Test 03UITests' do
    # Pods for testing
  end

end
